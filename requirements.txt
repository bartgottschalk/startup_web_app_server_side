# Django 5.2 LTS - UPGRADE COMPLETE! ✅
# Python 3.12.12 compatible versions
# Upgraded from Django 4.2.16 LTS to Django 5.2 LTS (supported until April 2028)

# Core Django
Django==5.2.9  # Django 5.2 LTS (supported until April 2028)

# Django Extensions
django-cors-headers==4.7.0  # Compatible with Django 5.2
django-import-export==4.3.14  # Compatible with Django 5.2
django-ratelimit==4.1.0  # Rate limiting for abuse prevention

# Utilities
configparser==5.2.0
titlecase==2.3.0
setuptools>=65.0.0  # Required for Python 3.12 (provides distutils)

# Payment Processing
stripe==14.0.1  # Upgraded from 5.5.0 for Stripe Checkout Sessions (December 2025)

# Database
psycopg2-binary==2.9.9  # PostgreSQL adapter for Python (binary version for easier installation)

# AWS Integration (for production deployment)
boto3==1.35.76  # AWS SDK for Python (Secrets Manager, S3, etc.)

# Production Server
gunicorn==21.2.0  # WSGI HTTP Server for production deployments
whitenoise==6.7.0  # Static file serving for Python web apps (Django Admin CSS)

# Testing (dev dependencies)
coverage==6.2
selenium==4.27.1  # Selenium 4.x with modern API (upgraded from 3.141.0 December 2025)

# Code Quality (linting)
flake8==7.3.0
pylint==4.0.2
