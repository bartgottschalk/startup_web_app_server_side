# Django 4.2 LTS - UPGRADE COMPLETE! ✅
# Python 3.12.12 compatible versions
# Upgraded from Django 2.2.28 (EOL 2022) to 4.2.16 LTS (supported until April 2026)

# Core Django
Django==4.2.16  # Step 6: Django 4.1 → 4.2 LTS (supported until April 2026)

# Django Extensions
django-cors-headers==4.4.0  # Compatible with Django 4.2
django-import-export==4.0.0  # Compatible with Django 4.2+

# Utilities
configparser==5.2.0
titlecase==2.3.0
setuptools>=65.0.0  # Required for Python 3.12 (provides distutils)

# Payment Processing
stripe==5.5.0  # Upgraded from 2.63.0 for Python 3.12 compatibility

# Database
psycopg2-binary==2.9.9  # PostgreSQL adapter for Python (binary version for easier installation)

# AWS Integration (for production deployment)
boto3==1.35.76  # AWS SDK for Python (Secrets Manager, S3, etc.)

# Testing (dev dependencies)
coverage==6.2
selenium==3.141.0  # Last 3.x version
urllib3<2.0.0  # Selenium 3.x requires urllib3 1.x

# Code Quality (linting)
flake8==7.3.0
pylint==4.0.2
