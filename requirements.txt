# Requirements for baseline testing (verified working state)
# Python 3.12.8 compatible versions
# All tests pass with these versions (see BASELINE_TEST_RESULTS.md)

# Core Django
Django==2.2.28  # Last 2.2.x release with security fixes

# Django Extensions
django-cors-headers==3.7.0  # Requires URLs with scheme in CORS_ORIGIN_WHITELIST
django-import-export==2.6.1

# Utilities
configparser==5.2.0
titlecase==2.3.0
setuptools>=65.0.0  # Required for Python 3.12 (provides distutils)

# Payment Processing
stripe==5.5.0  # Upgraded from 2.63.0 for Python 3.12 compatibility

# Testing (dev dependencies)
coverage==6.2
selenium==3.141.0  # Last 3.x version
