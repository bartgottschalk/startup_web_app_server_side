# Django Upgrade to 4.2 LTS - In Progress
# Python 3.12.12 compatible versions
# See docs/DJANGO_UPGRADE_PLAN.md for upgrade tracking

# Core Django
Django==4.1.13  # Step 5: Django 4.0 → 4.1 (last 4.1.x)

# Django Extensions
django-cors-headers==4.0.0  # Compatible with Django 4.1
django-import-export==3.3.0  # Compatible with Django 4.1+

# Utilities
configparser==5.2.0
titlecase==2.3.0
setuptools>=65.0.0  # Required for Python 3.12 (provides distutils)

# Payment Processing
stripe==5.5.0  # Upgraded from 2.63.0 for Python 3.12 compatibility

# Testing (dev dependencies)
coverage==6.2
selenium==3.141.0  # Last 3.x version
urllib3<2.0.0  # Selenium 3.x requires urllib3 1.x
